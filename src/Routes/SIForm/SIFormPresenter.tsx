/** @format */

import * as React from "react";
import SIForm from "../../Components/SIForm/SIForm";

const SIFormPresenter: React.SFC = (

) => (
  <SIForm>
  </SIForm>
);

export default SIFormPresenter;
